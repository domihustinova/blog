export enum Routes {
  Home = '/',
  Login = '/login',
  NotFound = '*',
}

export enum NavbarLinks {
  Recent = 'Recent Articles',
  MyArticles = 'My Articles',
  Create = 'Create Article',
  LogIn = 'Log in',
}
